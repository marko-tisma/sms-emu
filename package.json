{
  "name": "sms-emu",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && cp ./rom/* ./docs/",
    "benchmark:decoding": "tsc -p tsconfig.benchmark.json && node .benchmark-dist/benchmark/decoding_benchmark.js",
    "serve": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.3.2",
    "vite": "^8.0.9"
  }
}
